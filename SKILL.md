---
name: stamp-recognizer
description: "邮票识别：识别用户上传的邮票照片（单枚/信销票/整版多枚），返回志号、名称、发行日期、面值、简介和目录参考图。数据源为本地《中国邮票电子目录》。| Recognize Chinese stamps from photos: catalog number, name, issue date, denomination, description + reference image. Local offline catalog."
version: "1.0.0"
user-invocable: true
allowed-tools: Read, Write, Edit, Exec
---

# 邮票识别 / Stamp Recognizer

识别中国邮票照片（1949-2025 全覆盖，本地离线）。

## 触发方式
- 用户发送邮票照片（单枚、信销票带邮戳、整版/方连多枚均可）
- 用户说"识别这张邮票"、"这是什么邮票"、"帮我看看这枚邮票"等

## 使用流程
1. 保存用户上传的图片到临时路径（如 `projects/stamp-recognizer/tmp/input_<时间戳>.jpg`）
2. 执行识别：
   ```
   python tools/recognize.py <图片路径> --catalog catalog --json
   ```
3. 解析 JSON 输出，向用户呈现每枚票的结果（见输出格式）
4. **参考图输出（必须走 workspace 路径，G 盘等外部路径 webchat 不渲染）**：
   - 先把 `matched_image`（及 `matched_image_exact` 若存在）复制到 `projects/stamp-recognizer/tmp/ref_imgs/`
   - 用 `MEDIA:<复制后的路径>` 附参考图（每枚 ≤2 张）
   - **用后立即清理 ref_imgs/ 内容**（下次识别前清空 + 会话内用完即删，防止 C 盘积累）
   - 不完整区域不附图

## 输出格式（每枚票）
- **完整票（completeness=full）**：志号、名称、相似度（置信度）+ 完整字段（发行日期、面值含单枚明细、设计者、齿孔、尺寸、版别、印刷厂、发行量、简介）+ Top3 候选
- **完整票必附目录参考图（零成本高价值）**：`MEDIA:<matched_image>`（首选）；若 CLIP 精确命中图 `matched_image_exact` 存在且不同，优先附它；每枚最多 2 张。参考图是附件不占文字 token，用户看图秒判
- **不完整区域（completeness=partial）**：只标注「不完整」+ 原因（区域过小/宽高比异常/与已识别区域重复/含多个志号/与区域N重叠），给 3 个候选（不带全字段、不附图），并描述在图片中的**方位**（左上角/上方居中/中央/右侧居中…）
- 每个区域都带 `position`（九宫格方位）
- 志号、名称、相似度（置信度）
- 发行日期、面值（含单枚面值明细）、设计者
- **收藏信息：齿孔、尺寸、版别、印刷厂、发行量**（数据源为目录库 stamps 明细）
- 简介（截断 80 字）
- Top 3 候选防误判；无匹配时给出 OCR 文字作线索
- **成本纪律**：不输出"更多参考图"路径文本列表（省 token）；候选 2/3 只给志号/名称/分数（不给全字段）；MEDIA 图每枚 ≤2 张
- 千问精读时附 `vision` 字段（千问读出的志号/票名/年份/面值/票面文字）
- source 标注证据来源：`vision`（千问精读）/ `ocr`（票面文字检索）/ `clip_text`（图案描述检索）/ `clip`（图案匹配）

## 注意事项
- **OCR 用 RapidOCR**（onnxruntime，模型随包自带，无需联网下载；CPU 快）
- 纯 CPU 环境：单枚识别 2-6 秒，整版 N 枚 ≈ N 倍
- 单图最多拆 12 枚；图片建议拍正、光线均匀
- 匹配阈值：相似度 < 0.15 视为无匹配（可调）
- 识别不到时：提示用户换角度重拍，或说明“目录外品种”

## 文件结构（引擎在 projects/stamp-recognizer/）
```
projects/stamp-recognizer/
  tools/recognize.py     # 主入口（多枚拆图+OCR+匹配）
  tools/preprocess.py    # 邮票区域检测/矫正
  tools/ocr_engine.py    # RapidOCR 封装（模型随包自带）
  tools/embedder.py      # CLIP 嵌入
  tools/catalog.py       # 索引加载与匹配
  catalog/catalog.json   # 2,003 套票元数据
  catalog/index.npy      # 8,946 张图嵌入矩阵
  catalog/index.json     # 索引元数据
```

## 已知边界
- 识别精度为"套票级"（志号级），单枚图序定位为 v2 目标
- **CLIP 对"个"字头红底大字个性化票区分度差**：五福临门/岁岁平安/和谐/故宫等分数全部挤在 0.72-0.80，排名不可靠（2026-08-11 实测）
- **OCR 读不出书法/艺术字**：五福临门的"福"字读不到；但"贺龙同志诞生九十周年"这类印刷体可读，OCR 文字检索是可靠线索
- 实拍照片拆图：Canny（detect_stamp）经常失败，已加 OCR 文字框定位（detect_by_ocr）作 fallback
- 大图（4000px 级）直接 OCR 会内存溢出，需先缩放（已内置）
- 暂不做市场估价（StampWorld 询价接口已预留，v2 实现）

## v2.1 已实现（2026-08-12）
- **候选全部带参考图**：每个 match 含 `images`（目录参考图绝对路径，最多 6 张）+ `matched_image_exact`（CLIP 精确命中图）。人工确认从"读文字猜"变"看图秒选"，输出端用 MEDIA: 呈现
- **收藏信息全输出**：齿孔/尺寸/版别/印刷厂/发行量（来自 stamps 明细，98% 覆盖）
- **目录库重建**：修复 CHM 解析两大 bug（嵌套表格拍平污染表头 + 标签空格/全角空格未归一化），stamps 明细字段（stamp_no/stamp_name/denomination/size/perforation/mintage）全面修正——壬申年齿孔 11.5、J126 齿孔 11×11.5、贺年15-2020 齿孔 13 均正确
- **千问精读裁判**（`tools/vision_llm.py`）：OCR/CLIP 都没把握时（OCR 无字 / CLIP 分数扎堆 / 多候选并列），调通义千问 VL 精读票面，结构化输出志号/票名/年份/面值/票面文字/图案描述，再用这些字段精确检索本地目录
  - **千问只"读"不"编"**：最终信息一律取自本地 catalog.json，杜绝幻觉；志号看不清返回 null 绝不猜
  - 触发条件：`!_ocr_confident()` 且 `!_clip_confident()`（OCR 命中≥2词且领先，或 CLIP ≥0.80 且差距≥0.03，即跳过千问，零云成本）
  - 无 `DASHSCOPE_API_KEY` 环境变量时自动降级为纯本地流程，不报错
  - 可用 `--no-vision` 强制关闭；模型默认 `qwen-vl-max`（`VISION_MODEL` 可覆盖）
  - 待办：识别结果缓存（同图不重复烧钱）

## v2 已实现（2026-08-11）
- **OCR 文字检索优先**：票面印刷体文字 2-gram 直接查目录名称，命中即置顶（source=ocr）——贺龙→J126 实测有效
  - 噪音过滤：2-gram 含"中国邮政人民华共"任一字符即丢弃（修掉 set 顺序导致的"中国"残留 bug）
- **单枚面值明细**：输出 denominations_detail（如 2-1 国裕家康 1.20元 / 2-2 新春送福 3.00元），避免全套面值误导
- **低区分度警示**：Top1 与 Top2 分数差 < 0.02 时 low_discrimination=true，提示人工对照参考图
- 候选默认 Top5（--top 可调）

## 待办（v2 剩余）
- [x] 千问精读结果缓存（图 hash → 结果，同图不重复烧云 token）——2026-08-13 v2.4 已实现（.vision_cache/）
- 视觉模型正式 benchmark：真实邮票照片（印刷体/书法字/个性化票各若干张），统计各来源命中率（进行中）
- StampWorld 询价接口（tools/pricing.py 已预留）

## v2.2 已实现（2026-08-13，实测反馈）
- **完整性判定 + 方位输出**：拆图区域先判完整（区域过小/宽高比异常/与已识别区域重复/OCR 含多个不同志号 => partial），完整票才读全字段，不完整区域只给 3 候选 + 方位描述（九宫格：左上角/上方居中/中央/…），不再输出"第 N 枚"式罗列
- **候选收敛**：每区域最多 3 个候选，Top1 带全字段+参考图，2/3 简列
- 测试（册页多枚实拍）：8 区域 → 2 完整（蔡元培 J145 千问 106 分 / 环境保护 T127）+ 6 不完整（多志号混入/重复/细条），判定全部正确

## v2.6 已实现（2026-08-13 收官，8 轮实测汇总）
- **残片嫌疑判定**：形状正常但 OCR 只有邮政字样/面值（无票面关键词且无完整志号）→ partial（"OCR 无票面信息（残片嫌疑）"），修掉"中国人民邮政"残片误判 full 的边界

## 已知边界（实测确认，非 bug）
- **密集册页拆图**：3 行 12+ 枚的册页，Canny 只能拆出 1-2 枚（巨框吞票/掩膜残留），已尽力（IoU 合并+OCR 定位切换），根治需**单枚拍摄**
- **黑白低对比度票**（如 J122 邹韬奋）：OCR 无字 + 千问视觉失败（肖像读成山峦/年份 1985↔1995 摇摆/面值幻觉）→ 诚实降级 uncertain，需单枚高清/扫描
- **CLIP 区分度**："个"字头红底大字个性化票分数扎堆（0.72-0.80），排名不可靠 → 靠千问志号/人工看图
- **千问偶发失败**：API 偶尔返回 None（缓存兜底，重测即命中）

## 2026-08-13 完整修复清单（8 轮实测）
1. 数据迁移 G 盘 + data_root 更新
2. 千问链路打通：key 持久化（环境变量 + .env）、数组输入支持、志号归一化（剥括号）
3. 完整性判定 v2.2：方位输出、候选收敛 Top3、区域过小/过大/宽高比/多志号/重复/重叠 六类降级
4. "重复"语义 bug（元素比较 vs 子串检查）+ 几何重叠检测
5. 密集册页：IoU 重复合并 + Canny 单枚时自动切 OCR 定位
6. 千问误读防御：票名一致性校验降权（T.128→T.126 案例）、_sanitize_vision（面值/年份校验）、prompt 强化
7. CLIP 文本检索（无文字票第 4 证据线）+ 年份软加权
8. OCR 检索范围扩展（stamps 明细纳入）
9. token 优化：形状预筛（千问 8→2 次）、.vision_cache 缓存（重测 0 调用）、图片 1280/85、max_tokens 400
10. 输出：MEDIA 参考图走 workspace 路径（G 盘不渲染）、用后清理、成本纪律（多贴图少写字）

## v2.4 已实现（2026-08-13，token 优化）
- **形状预筛**：细长条/巨框/碎块区域（且 OCR 无完整志号）直接降级简检，只跑 OCR 给候选，跳过 CLIP+千问——册页图实测千问调用 **8→2 次**
- **千问结果缓存**：图感知 hash(64px) → 结果 JSON（`.vision_cache/`，gitignore 排除），同图重测/重复区域 **0 API 调用**（兔票图重测 13s 纯本地）
- **图片压缩**：max_side 1600→1280、quality 90→85（视觉 token -30~40%）；**max_tokens 600→400**

## v2.3 已实现（2026-08-13，邹韬奋票测试后修复）
- **CLIP 文本检索（无文字票新证据线）**：OCR 无字 + 千问读不出志号时，用千问的图案描述（design）做文本嵌入去图库检索（embed_text），证据源 clip_text，排序在 clip 之前
- **年份软加权**：图案检索结果里年份匹配的 +1.0 加分，不硬过滤——千问可能误读年份（1985↔1995），硬过滤会误杀正确答案
- **vision_search 噪音过滤**：关键词 2-gram 含"中国邮政人民华共"任一字符即丢弃（修掉"中国人民邮政"→命中《中国人民之友》的污染）
- **千问输出校验**（_sanitize_vision）：面值必须带单位且区间合理（拦 9.45元 幻觉）；年份必须 4 位数字（拦"一八六八—一九八八"）
- **prompt 强化**：design 描述放宽到 80 字且要求具体（人物姿态/服饰/色调，用于图案检索）；面值必须带单位
- 已知边界：黑白低对比度票（如 J122 邹韬奋）千问视觉失败（肖像读成山峦/年份摇摆）→ 正确做法是单枚高清拍摄，链路会诚实标 uncertain

## 2026-08-13 修复记录
- **数据迁移**：目录库数据已迁移至独立数据盘（_extracted 解压数据 + chm 原始档），catalog.json data_root 指向数据目录
- **千问 key 持久化**：DASHSCOPE_API_KEY 用户环境变量 + 项目根 `.env` 兜底（vision_llm.py 自动读取，优先级环境变量 > .env）
- **bug1：vision_read_stamp 不支持 numpy 数组**——recognize.py 传入裁剪后的 cv2 数组，Image.open(数组) 失败导致千问静默降级。已修：数组自动转 RGB（灰度/BGRA 分支）
- **bug2：志号归一化不剥离括号/点号**——千问读出 J.146.(2-1) 匹配不上目录 J146。已修：括号内容（图序）剥离 + 去符号 + 精确匹配+100 / 长前缀(≥3)+50
- **索引核对**：9308 是引用数，按内容 hash 去重后唯一图片 8946，与旧索引一致，无缺失

## 踩坑（2026-08-11 实测修复）
- huggingface.co 在国内网络不可达：transformers 加载 CLIP 时会联网检查远程权重（即使本地已有缓存），WinError 10060 卡死/重试
- 修复：`embedder.py` 顶部 `os.environ.setdefault("HF_HUB_OFFLINE", "1")` + `TRANSFORMERS_OFFLINE=1`，强制用本地缓存（snapshots 下 pytorch_model.bin / model.safetensors 约 605MB 已在本地）
- 后续运行识别无需再手动设置环境变量
